# Sentiment-Analyses-Model
A sentiment analysis model that classifies Twitter data into Positive, Negative, and Neutral categories using Logistic Regression. The model utilizes TF-IDF for text vectorization and is optimized via GridSearchCV. Deployed as a Streamlit web app, allowing real-time tweet sentiment prediction.
